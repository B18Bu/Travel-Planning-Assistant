# 旅行轮播图片

将轮播图片保存到此目录。前端会按以下顺序优先尝试加载：

1. `travel-mountain.webp` / `.jpg` / `.png`
2. `travel-coast.webp` / `.jpg` / `.png`
3. `travel-ancient-city.webp` / `.jpg` / `.png`

图片尚未上传或加载失败时，页面会继续显示对应的 CSS 场景占位，不会影响旅行规划功能。

建议使用横向图片，比例接近 `16:9`，单张文件控制在 2 MB 以内。
